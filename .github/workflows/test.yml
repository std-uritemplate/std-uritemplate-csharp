name: Test

on:
  push:
  # TEST: REVERT ME
  #   branches:
  #     - main
  # pull_request:
  #   paths-ignore:
  #     - '**.md'

jobs:
  test:
    # TODO: fix me
    # uses: std-uritemplate/std-uritemplate/.github/workflows/test.yml@main
    uses: andreaTP/std-uritemplate/.github/workflows/test.yml@extract-csharp
